===line===
{{Indent}}- **The owner's own voice.** They have named a voice skill: load `{{VoiceSkill}}` and its `{{VoiceContext}}` context before you write a word of this, and write it the way they write. The repository's own PR-description rule and this project's writing conventions still decide the structure; the voice skill decides only the prose.
