===heading===
# Independent pre-PR review — cycle {{Cycle}}
===verify-mode-preamble===
This cycle dispatched one reviewer standing in for every still-active track,
reading the delta since the prior cycle rather than the whole diff. A finding
belongs to whichever track its own tag names, not to the section heading below.
===discovery-mode-preamble===
Each section below is one independent pass over the same diff, with its own fresh
context. A finding belongs to the lens whose section it appears under.
===pass-heading===
## {{LensHeading}} — verdict: {{VerdictLabel}}
===no-output===
(this pass recorded no output)
