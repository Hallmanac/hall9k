===body===
Your previous session ended with an error partway through — most likely a transient
provider-side hiccup, not a problem with the work itself. Resume exactly where you left
off — check `git status` and `git diff` for anything uncommitted — and continue toward
the acceptance criteria you were already given. Do not restart or re-derive work already done.
