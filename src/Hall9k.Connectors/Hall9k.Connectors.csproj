<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <Using Include="Hall9k.Domain.Infrastructure.Extensions" />
    <!-- StreamState moved from Marten.Events to JasperFx.Events under Marten 9 (migration guide, "Events.IEvent moves"); this alias keeps every call site unchanged. -->
    <Using Include="JasperFx.Events.StreamState" Alias="StreamState" />
  </ItemGroup>

  <ItemGroup>
    <!-- Worktree/prompt services (shared with the CLI, so they cannot pull in Microsoft.Extensions.Hosting) need ILogger<T> -->
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Hall9k.Domain\Hall9k.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Hall9k.Tests" />
  </ItemGroup>

</Project>
